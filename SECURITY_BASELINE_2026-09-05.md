# تقرير خط أساس الأمان — 2026-09-05

## نتائج الفحص قبل التعديل

1. **تهيئة المصادقة غير موحدة** — `app.py` يستورد طبقة قاعدة البيانات لكنه لا يهيئ Auth بنفسه. التهيئة الموجودة في `wsgi_config.py` تعتمد على تشغيل مسار Gunicorn محدد، بينما `run.bat` يشغّل `app.py` مباشرة. لذلك لا توجد ضمانة واحدة بأن المسارات المحلية والسحابية تطبق نفس الحماية.
2. **كلمات المرور ليست bcrypt/Argon2** — `auth_bootstrap.py` و`auth_pg.py` يستخدمان PBKDF2-HMAC-SHA256 يدويًا مع 310,000 تكرار. هذا أفضل من plaintext، لكنه لا يحقق المتطلب الحالي المطلوب للتوحيد على bcrypt/Argon2، كما أن منطق hashing مكرر في ملفين.
3. **خطة ترحيل كلمات المرور غير مكتملة** — يوجد أكثر من مخزن مستخدمين (SQLite محلي وPostgreSQL سحابي)، و`auth_pg.py` يستطيع ترحيل hash القديم كما هو. لا توجد آلية واضحة لترقية hash القديم تلقائيًا إلى scheme أحدث بعد نجاح تسجيل الدخول.
4. **حماية CSRF غير موجودة كطبقة موحدة** — نماذج تسجيل الدخول وصفحات الإدارة وطلبات JSON تغيّر الحالة، ولا توجد middleware/طبقة تحقق CSRF عامة قبل الطلبات.
5. **الصلاحيات ليست موحدة في طبقة واحدة** — `auth_security_extensions.py` و`auth_security_extensions_v2.py` يحتويان قواعد متداخلة؛ `v2` يضيف منعًا لبعض عمليات الموظف، لكن تفعيله يعتمد على wiring خارجي. هذا يخلق خطر اختلاف السلوك بين بيئات التشغيل.
6. **عمليات حساسة قابلة للتنفيذ عند غياب طبقة الحماية** — `POST /api/data/reset` و`POST /api/backups/restore` موجودتان في `app.py`، والحماية لهما موجودة كـ `before_request` في امتدادات Auth لا في route نفسها.
7. **تسريب معلومات في الأخطاء والـlogs** — بعض handlers يعيدون `str(e)` مباشرة في JSON للمستخدم، كما أن `app.logger.exception(...)` يحتفظ بتفاصيل الاستثناء كاملة. يلزم تنقيح الرسائل ومنع ظهور أرقام الجوالات أو محتوى الطلبات داخل رسائل الاستثناء/التدقيق.
8. **POST/PUT/DELETE كثيرة تعتمد على session auth فقط** — لا يوجد CSRF token مطلوب على مستوى جميع state-changing endpoints.
9. **ملفات مصرفة داخل المستودع** — توجد ملفات `.pyc` داخل الجذر و`__pycache__/`، وهي ضوضاء repository ومصدر محتمل لعدم تطابق الكود مع المصدر.
10. **تراكم ملفات fix وpatch** — توجد طبقات patch مستقلة (`cloud_db_update_fix.py`, `pending_availability_fix.py`, `data_merge_safety.py`, `cloud_integration_safety.py`, `postrollback_export.py`) تتدخل في runtime بجانب wiring آخر، ما يصعّب إثبات مسار التنفيذ الفعلي.

## ملاحظات مهمة

- لا توجد في الكود الذي تمت مراجعته مؤشرات على كلمات مرور مخزنة plaintext؛ المشكلة هي تعدد التنفيذ والاعتماد على PBKDF2 اليدوي وليس plaintext.
- `postrollback_export.py` يستخدم فحص session للمدير، لكن وجوده كامتداد runtime منفصل يزيد خطر اختلاف الحماية عن بقية المسارات.
- هذا الملف يمثل baseline فقط. الإصلاحات التالية ستُطبّق في commits صغيرة وقابلة للمراجعة.