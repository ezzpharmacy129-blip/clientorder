# تشغيل محلي بدون إنترنت

## الهدف
- تشغيل تطبيق Flask على كمبيوتر الصيدلية من CMD فقط.
- حفظ البيانات في Excel على الكمبيوتر.
- حفظ صور الطلبات محليًا على الكمبيوتر.
- السماح للجوالات بالوصول عبر شبكة الصيدلية الداخلية فقط.
- لا يحتاج التطبيق إلى Aiven أو Render أو Koyeb أو Supabase أثناء التشغيل المحلي.

## التشغيل
1. ثبّت Python مرة واحدة على الكمبيوتر.
2. من مجلد المشروع ثبّت الاعتمادات مرة واحدة:
   `python -m pip install -r requirements.txt`
3. شغّل `START_LOCAL.bat`.
4. اترك نافذة CMD مفتوحة.
5. من نفس الكمبيوتر افتح `http://127.0.0.1:5000`.
6. من جوال متصل بنفس Wi-Fi افتح `http://PC-IP:5000`، واستبدل PC-IP بعنوان IPv4 الذي يظهر في CMD.

## التخزين
- البيانات الرئيسية: `EzzPharmacyData\data\pharmacy_orders.xlsx`
- النسخ الاحتياطية: `EzzPharmacyData\backups\`
- الصور: `EzzPharmacyData\uploads\`

يمكن تغيير مجلد البيانات عبر متغير البيئة `EZZ_PHARMACY_DATA_DIR`.

## ملاحظات
- الإنترنت غير مطلوب أثناء تشغيل النظام داخل الشبكة المحلية.
- الكمبيوتر الرئيسي يجب أن يبقى مشغّلًا ومرتبطًا بشبكة Wi-Fi/LAN.
- لا تجعل منفذ 5000 متاحًا للإنترنت العام.
