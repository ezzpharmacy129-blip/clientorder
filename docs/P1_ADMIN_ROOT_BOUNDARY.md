# P1 — Admin root boundary

The `/admin` route is part of the administrative surface and must require the `admin` role just like `/admin/*` routes.

This hardening change does not modify database schema or data.
