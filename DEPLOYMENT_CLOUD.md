# تشغيل صيدلية عز الصحة على سيرفر مجاني

## البنية
- Flask + Gunicorn للتطبيق.
- PostgreSQL (مفضل: Supabase Free) لقاعدة البيانات.
- لا يوجد تسجيل دخول أو صلاحيات حسب الطلب.
- الصور والنسخ الاحتياطية تُحفظ داخل قاعدة البيانات، لذلك لا نعتمد على القرص المؤقت للسيرفر.
- يمكن فتح نفس الرابط من جوال الموظف ومن جهاز الصيدلية في نفس الوقت.

## 1) إنشاء قاعدة البيانات
أنشئ مشروع PostgreSQL مجاني، ويفضل Supabase. خذ **Connection string** الخاص بـ PostgreSQL.

## 2) إنشاء خدمة الويب
ارفع هذا المشروع إلى GitHub ثم أنشئ Web Service في Render واختر المستودع.
البرنامج يحتوي على `render.yaml` و`requirements.txt` لذلك الإعدادات الأساسية جاهزة.

## 3) متغير البيئة الوحيد
في إعدادات الخدمة أضف:

`DATABASE_URL`

والقيمة هي رابط PostgreSQL الذي حصلت عليه من Supabase.

## 4) الاختبار
بعد النشر افتح:

`/health`

ويجب أن يرجع `ok: true` مع نوع التخزين `postgresql`.

## 5) الاستيراد من النسخة المحلية
من داخل البرنامج افتح «إدارة البيانات» → «استيراد بيانات قديمة»، وارفع:
- ملف `.xlsx` القديم، أو
- ملف `.zip` من النسخ الاحتياطية القديمة.

قبل الاستيراد يأخذ النظام نسخة احتياطية من الحالة السحابية الحالية عندما توجد بيانات محفوظة.

## ملاحظة عن المجاني
الخطة المجانية في الاستضافة وقاعدة البيانات لها حدود استخدام وموارد ونوم/إيقاف حسب مزود الخدمة. هذه النسخة مصممة بحيث لا تعتمد على ملفات القرص المؤقت، لكن توفر الخدمة نفسها يبقى خاضعًا لسياسة الخطة المجانية لدى المزود.
