name: Obsolete shortage WhatsApp patch (disabled)

on:
  workflow_dispatch:

# Intentionally disabled. The shortage WhatsApp-note behavior is now in source.
# This workflow must not modify production source code or push to main.
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "This one-time shortage WhatsApp patch workflow is retired."
          echo "No source-code patching or git push is performed."