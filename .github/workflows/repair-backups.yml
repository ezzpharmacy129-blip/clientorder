name: Obsolete backup repair (disabled)

on:
  workflow_dispatch:

# Retired duplicate one-time repair. The application source is the
# canonical implementation; CI must not rewrite static/app.js and push.
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This one-time backup repair workflow is retired."