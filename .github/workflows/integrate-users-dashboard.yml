name: Obsolete users dashboard integration patch (disabled)

on:
  workflow_dispatch:

# Intentionally disabled. The users dashboard is now part of the source tree.
# This workflow must not modify production source code or push to main.
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "This one-time users dashboard integration workflow is retired."
          echo "No source-code patching or git push is performed."