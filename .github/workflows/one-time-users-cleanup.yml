name: Retired one-time users dashboard cleanup

on:
  workflow_dispatch:

# Retired: this workflow previously modified source files and pushed to main.
# It is intentionally kept as a disabled manual stub for audit/history only.

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired workflow; no source mutation or push is permitted."
