name: Obsolete backup patch (disabled)

on:
  workflow_dispatch:

# Retired one-time patch. Backup behavior must live in application source,
# not in a workflow that rewrites static/app.js and pushes to main.
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This one-time backup patch workflow is retired."